/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // React strict mode'u aktif tutuyoruz
  // `swcMinify` kaldırıldı çünkü Next.js 15.2.4 sürümünde bu seçenek geçersiz
};

module.exports = nextConfig;
